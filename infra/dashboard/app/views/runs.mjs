// Placeholder for Task 5/7. Enough to let Task 4's router render and be verified in a browser;
// a later task replaces this export with the real run/chat view.
import { html } from '../ui.mjs';
export const RunView = ({ name, run }) => html`<div class="tk-stub">run view: ${name} / ${run} — pending later task</div>`;
